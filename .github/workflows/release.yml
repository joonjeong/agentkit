name: Release

on:
  workflow_call:
    inputs:
      target_ref:
        description: "Git ref or SHA to build and attach to the release."
        required: true
        type: string
      head:
        description: "LINE HeadVer head value."
        required: false
        default: "0"
        type: string
      build:
        description: "LINE HeadVer build value."
        required: true
        type: string
      timezone:
        description: "Timezone used for LINE HeadVer yearweek."
        required: false
        default: "Asia/Seoul"
        type: string

permissions:
  contents: write

jobs:
  prepare:
    name: Prepare release
    runs-on: ubuntu-latest
    outputs:
      asset_suffix: ${{ steps.metadata.outputs.asset_suffix }}
      release_name: ${{ steps.metadata.outputs.release_name }}
      release_notes: ${{ steps.metadata.outputs.release_notes }}
      release_tag: ${{ steps.metadata.outputs.release_tag }}
      release_version: ${{ steps.metadata.outputs.version }}
      target_sha: ${{ steps.metadata.outputs.target_sha }}
    steps:
      - name: Checkout target
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_ref }}

      - name: Resolve release metadata
        id: metadata
        shell: bash
        run: |
          set -euo pipefail
          scripts/release-metadata \
            --target-ref "${{ inputs.target_ref }}" \
            --head "${{ inputs.head }}" \
            --build "${{ inputs.build }}" \
            --timezone "${{ inputs.timezone }}" >> "${GITHUB_OUTPUT}"

  build:
    name: Build ${{ matrix.asset }}
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            asset: agent-session-x86_64-unknown-linux-musl
            binary: agent-session
            build_tool: cargo-zigbuild
          - os: ubuntu-latest
            target: aarch64-unknown-linux-musl
            asset: agent-session-aarch64-unknown-linux-musl
            binary: agent-session
            build_tool: cargo-zigbuild
          - os: macos-14
            target: aarch64-apple-darwin
            asset: agent-session-aarch64-apple-darwin
            binary: agent-session
            build_tool: cargo
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            asset: agentd-x86_64-unknown-linux-musl
            binary: agentd
            build_tool: cargo-zigbuild
          - os: ubuntu-latest
            target: aarch64-unknown-linux-musl
            asset: agentd-aarch64-unknown-linux-musl
            binary: agentd
            build_tool: cargo-zigbuild
          - os: macos-14
            target: aarch64-apple-darwin
            asset: agentd-aarch64-apple-darwin
            binary: agentd
            build_tool: cargo
    uses: ./.github/workflows/build.yml
    with:
      os: ${{ matrix.os }}
      target: ${{ matrix.target }}
      build_tool: ${{ matrix.build_tool }}
      agent_session_version: ${{ needs.prepare.outputs.release_version }}
      binary_name: ${{ matrix.binary }}
      ref: ${{ needs.prepare.outputs.target_sha }}
      artifact_name: ${{ matrix.asset }}-${{ needs.prepare.outputs.asset_suffix }}
      upload_artifact: true

  publish:
    name: Publish release
    needs: [prepare, build]
    runs-on: ubuntu-latest
    steps:
      - name: Download release assets
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Create GitHub release with assets
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RELEASE_NAME: ${{ needs.prepare.outputs.release_name }}
          RELEASE_NOTES: ${{ needs.prepare.outputs.release_notes }}
          RELEASE_TAG: ${{ needs.prepare.outputs.release_tag }}
          TARGET_REF: ${{ needs.prepare.outputs.target_sha }}
        run: |
          set -euo pipefail

          if gh release view "${RELEASE_TAG}" >/dev/null 2>&1; then
            echo "Release ${RELEASE_TAG} already exists; immutable releases cannot be amended." >&2
            exit 1
          fi

          shopt -s nullglob
          assets=(dist/*)
          if [[ ${#assets[@]} -eq 0 ]]; then
            echo "No release assets found in dist." >&2
            exit 1
          fi

          gh release create "${RELEASE_TAG}" "${assets[@]}" \
            --target "${TARGET_REF}" \
            --title "${RELEASE_NAME}" \
            --notes "${RELEASE_NOTES}"
